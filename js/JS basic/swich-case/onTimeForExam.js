function onTime(input) {
    
    let examHour = Number(input[0]);
    let examMin = Number(input[1]);
    let arrivalHour = Number(input[2]);
    let arrivalMin = Number(input[3]);

    let totalMinAr = (arrivalHour / 60) + arrivalMin;
    let totalMinEx = (examHour / 60) + examMin;
    let totalHourAr = Math.floor();
    let min = 0;

    if( totalMinEx >= totalMinAr){
        h = 

        console.log('On time');
        if(totalMinEx - totalMinAr <1){
            console.log(``);
        }  
    }
}
onTime(['9', '00', '8', '30'])